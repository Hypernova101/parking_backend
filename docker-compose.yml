version: '3'
services:
        web:
                image: parking
                build: .
                env_file:
                        - .env
                ports:
                        - "8000:8000"
                volumes:
                        - ./instance:/instance
                restart: unless-stopped
