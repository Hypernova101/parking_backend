version: '3'
services:
        web:
                image: parking
                build: .
                env_file:
                        - .env # This file is optional; defaults will be used if it does not exist
                ports:
                        - "8000:8000"
                volumes:
                        - ./instance:/instance
                restart: unless-stopped
